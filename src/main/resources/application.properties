spring.application.name=webapp
server.port=${PORT:8080}

# MongoDB Configuration - Use environment variables for production
spring.data.mongodb.uri=${MONGODB_URI:mongodb+srv://guest:C26nkuO9vYCmpwnC@cluster0.scyxx.mongodb.net/?appName=Cluster0}
spring.data.mongodb.database=${MONGODB_DATABASE:webapp}
spring.data.mongodb.auto-index-creation=true

# JWT Configuration - Use environment variables for production
jwt.secret=${JWT_SECRET:mySecretKeyForJWTThatShouldBeAtLeast32CharactersLongForHS512Algorithm}
jwt.expirationMs=${JWT_EXPIRATION_MS:86400000}

# Logging Configuration
logging.level.root=INFO
logging.level.com.example.webapp=${LOG_LEVEL:DEBUG}
logging.level.org.springframework.security=DEBUG

# Security Configuration
spring.security.filter.order=5
