# Backend Environment Variables
# Set these as environment variables in your deployment platform (Railway, Render, etc.)

# MongoDB Connection (use your MongoDB Atlas connection string)
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/?appName=YourApp
MONGODB_DATABASE=webapp

# JWT Configuration
JWT_SECRET=your-very-long-secret-key-at-least-64-characters-for-production-security
JWT_EXPIRATION_MS=86400000

# Server Configuration
SERVER_PORT=8080

# CORS - Set to your frontend URL in production
CORS_ALLOWED_ORIGINS=https://your-frontend.vercel.app
